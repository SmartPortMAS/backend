# 온산 MVP 구현 검토 패키지 (2026-07-20)

작성: 함현우 (검토 요청 대상: 김동안 님 — backend/Neo4j 담당)

온산 액체화물 하역 관제 MVP 설계 산출물(`onsan_mvp/` — 설계문서 6종 + 시드 CSV 10종 + 스크립트 6종)을
로컬 프로토타입에 **구현·검증까지 완료**한 상태입니다. 정식 backend(`SmartPortMAS/backend`, app/ 구조)로의
이식 방법을 검토받고 싶습니다. 아직 git에 올리지 않았고, 검토 후 Draft PR로 올릴 예정입니다.

---

## 1. MVP 대상 범위 — 왜 온산항인가

- **대상: 온산항 전체 액체 부두** — 액체화물현황(해수부 805) 기준 12개 부두 + 부이 3기, 15시설/26선석, 접안능력 합계 1,704,000 DWT. 본항 SK·신항은 Phase 2로 미룸.
- **수역 기반 범위 설정**: 수역(처용리/산암리)은 VTS와 정박지 배정이 실제로 쓰는 규제·운영 경계라 "왜 이 선석들만?"에 대한 답이 명확함. 혼재/근접 위험은 물리적 이웃을 빠짐없이 모델링해야 잡히므로, 수역이 그 완결 경계가 됨.
- **벌크(온산1~4)는 액체 목록에 없어** 화학 특화라는 차별점이 희석되지 않음.
- **처용리와 산암리는 약 2.5km 떨어진 별개 클러스터** → 인접(ADJACENT_TO)은 수역 소속이 아니라 좌표 거리(임계 500m)로 판정. OTK와 정일이 직접 인접으로 묶이지 않아 서로 다른 두 테스트 케이스가 생김.
- 상세 근거: `onsan_mvp/docs/온산항_MVP범위_안전규칙시드.md` 1장, `README.md` 1장.

---

## 2. 구현 내역 요약 (HANDOFF P2 → P1 → P3 → P5+P4 순서로 전부 완료)

| 단계 | 내용 | 구현 파일 | 검증 |
|---|---|---|---|
| P2 | 선석별 기상 3단계 판정 (정상/하역중단/이안/호스분리 + 판단불가 fail-safe) | `backend/ai/weather_logic.py`, `/api/v1/weather/assess` | 10/10 PASS |
| P1 | Neo4j 온산 스코프 적재 (Berth 15, ADJACENT_TO 20, SUBSTITUTABLE_WITH 18) + 스케줄링 후보풀 `mvp_berth_ids()` 제한 | `backend/graph/load_onsan_graph.py`, `backend/simulator/data_generator.py` | 적재 완료·Cypher 확인 |
| P3 | Anchorage 7 + FALLBACK_ANCHORAGE 12 적재, 전용→대체→정박지대기 배정 로직 (product/dwt/draught 게이트) | `backend/ai/berth_assignment.py`, `backend/ai/orchestrator.py` | 10/10 PASS |
| P5+P4 | 안전 게이트 R1~R15 결정론 구현 + risk_level 결정론화 (GAP5) | `backend/ai/safety_gates.py`, `POST /api/v1/safety/assess` | 18/18 PASS |

설계 원칙 준수:
- onsan_mvp/scripts 의 함수(`assess_berth_weather`, `mvp_berth_ids`, `assign_anchorage`)를 **그대로 import**해 재사용 (재구현 없음)
- ADJACENT_TO(혼재)와 SUBSTITUTABLE_WITH(대체)는 **별도 관계로 유지**
- 위험 탐지는 결정론(룰+그래프), 같은 입력 → 같은 risk_level (5회 반복 동일 출력 확인). LLM 자리는 `compose_explanation()` 문장 생성만
- DB 접속정보는 환경변수(`NEO4J_URI/USER/PASSWORD`)로만 읽음, 하드코딩 없음
- 좌표 결측 3개(달포, SPM 부이 2기)는 `coord_missing=true` 표시만 하고 진행 (사람 확보 대상)

---

## 3. 실제 API 응답 샘플 (로컬 컨테이너 http://localhost:8000 실측)

### 3-1. 선석별 기상 — 같은 기상인데 선석마다 판정이 갈림 (GAP6 해소)

`GET /api/v1/weather/assess?berth_group=정일1/2부두(산암리)&wind_speed=13&wave_height=1.2`

```json
{
  "berth_group": "정일1/2부두(산암리)",
  "status": "하역중단",
  "reasons": ["파고 1.2 m >= 1.0 m -> 하역중단"],
  "thresholds_used": {
    "stop": {"wind": 17.0, "wave": 1.0},
    "unberth": {"wind": 19.0, "wave": null},
    "disconnect": {"wind": 21.0, "wave": 2.0},
    "source": "정일_입항정보_9.8"
  }
}
```

같은 입력을 OTK1/2부두에 주면 **"정상"** (OTK 중단 임계: 풍속 14 / 파고 2.0).
기존 단일 임계(14/1.5)면 둘 다 "가능"으로 오판하던 케이스. `is_stale=true` 넘기면 "판단불가" fail-safe.

### 3-2. 안전 게이트 — 벤젠 입항, 인접 선석 나프타 하역중

`POST /api/v1/safety/assess` (벤젠 / OTK 1부두 / 인접 OTK 2부두 나프타 하역중)

```json
{
  "risk_level": "위험",
  "risk_level_basis": {
    "rule_engine_floor": "안전",
    "imdg_segregation_code": 2,
    "flammability_grade": "저인화점",
    "gate_hits": ["R13", "R15"]
  },
  "gates_hit": [
    {"rule": "R13", "severity": "HOLD",
     "reason": "인접 'OTK 2부두' 하역중 '나프타' - 거리 0.0m < IMDG 격리코드 2 요구 6m -> 증기운 중첩 위험, 배정 보류"},
    {"rule": "R15", "severity": "HOLD",
     "reason": "벤젠 100.0% 초과 화물 - 이전화물 벤젠프리 증명 필요, 미제출 -> 보류"}
  ],
  "explanation": {
    "summary": "위험등급 '위험': 게이트 15개 중 2건 히트",
    "checklist": ["하역 전 정전기 방지 접지 확인", "...", "벤젠 함유 화물 특별취급 절차 확인 (정일 9.18 / OTK 9.17)", "H2S 발생 가능성 및 가스 감지 확인 (정일 9.19 / OTK 9.18)"]
  }
}
```

게이트 15개는 매 호출 전부 실행. SIRE 없는 유조선 → R1 히트 → "배정불가"(하드블록)도 확인됨.

### 3-3. 오케스트레이터 — 전용→대체→정박지대기 판단 경로

`GET /api/agent/orchestrate?berth_name=효성 부두&cargo_name=에탄올&dwt=325000`

```json
{
  "status": "WAITING_ANCHORAGE",
  "anchorage": "부이직접/외해대기",
  "berth_decision": {
    "path": "정박지대기",
    "trace": [
      "전용 선석 '효성 부두' 점유 중 (현재 GREEN OCEAN/경유)",
      "'효성 부두'은 단독선석(대체 불가) -> 대체 탐색 없이 정박지 대기",
      "assign_anchorage(dwt=325000.0) -> '부이직접/외해대기' 대기"
    ]
  }
}
```

대체 성공 케이스: OTK1 점유 + DWT 9,000 → OTK2 대체 배정 (게이트: 화물계열/DWT/흘수 통과 근거 trace 포함).
게이트 탈락 케이스: DWT 20,000이면 "OTK 2부두 탈락: DWT 20,000 > 최대 10,000" → E2 대기.

---

## 4. Neo4j 적재 결과 (bolt://localhost:7687, 멱등 재실행 가능)

```
berth_nodes: 15  (온산 스코프, 구 파일럿 8선석 삭제됨)
adjacent_to_directed: 20      ← 혼재/근접 위험용 (distance_m 속성)
substitutable_with_directed: 18  ← 선석 대체용 (operator/shared_products/to_max_dwt/to_depth_m)
anchorage_nodes: 7  (E1/E2/E3/W1/B1~B3)
fallback_anchorage_edges: 12  (+ VLCC 부이 3기는 anchorage_fallback_note='부이직접/외해대기')
```

확인 쿼리 예: `MATCH (:Berth {berth_id:'CY-OTK1'})-[e:ADJACENT_TO]->(b) RETURN b.berth_name, e.distance_m`
→ OTK 2부두(0.0m), 대한유화 부두(279.7m)

---

## 5. 재현 방법 (내 로컬에서 바로 시연 가능)

```
cd 울산항만_프로젝트_최종본
docker compose up -d            # postgres + neo4j + backend
set NEO4J_PASSWORD=password
python backend\graph\load_onsan_graph.py   # 그래프 적재 (멱등)
```

- Swagger: http://localhost:8000/docs (새 엔드포인트 3개 + orchestrate 확장)
- Neo4j Browser: http://localhost:7474 (neo4j/password)
- 검증 스크립트 dry-run: `python backend/graph/load_onsan_graph.py --dry-run` (CSV 정합성 검사 포함)

---

## 6. 정식 backend(app/ 구조) 이식 가이드 — 검토 포인트

모듈이 전부 순수 Python + CSV 로드라 이식 부담이 작습니다. 제안:

| 프로토타입 모듈 | 이식 위치 제안 | 의존성 |
|---|---|---|
| `ai/weather_logic.py` (선석별 판정 부분) | `app/services/weather_service.py` | onsan_mvp/scripts/weather_berth_agent + thresholds CSV |
| `ai/berth_assignment.py` | `app/services/berth_assignment.py` | substitutability/summary CSV + assign_anchorage |
| `ai/safety_gates.py` | `app/services/safety_gates.py` | cargo_cas_map/berth_restrictions/adjacency CSV |
| `graph/load_onsan_graph.py` | `app/` 외부 스크립트 or alembic 유사 시드 단계 | 기존 `app/neo4j_client.py` 재사용 가능 |
| 엔드포인트 3개 | `app/api/v1/weather.py`, `safety.py`, `scheduling.py` | 위 서비스들 |

검토 요청 사항:
1. `onsan_mvp/` 폴더(설계문서+CSV+스크립트)를 backend 레포 루트에 커밋하는 방안 (HANDOFF 0단계 지시사항) — 위치 의견?
2. CSV 시드를 유지할지, Postgres 테이블로 옮기고 서비스가 DB를 읽게 할지 (MVP는 CSV로 충분하다고 판단했는데 의견?)
3. 안전 게이트의 선박 검사이력(SIRE/CDI)·GT 등 입력 필드 — AIS/PORT-MIS 모델에서 채울 수 있는 것과 수기 입력으로 남길 것 구분
4. 프로토타입의 시뮬레이터(BERTH_LIST 점유상태)를 정식 backend에서는 무엇으로 대체할지 (실데이터 선석 점유 추정?)
5. Draft PR로 올릴까요, 화면공유 리뷰 먼저 할까요?

---

## 변경 이력 (2026-07-21) — 좌표 결측 3개 확정 처리

발송본(07-20) 대비 아래가 바뀌었습니다. 본문 숫자 중 "Berth 15"는 **14**로 읽어주세요.

- **달포 부두(DP-DALPO)를 액체 MVP 대상에서 제외** — 위경도가 어떤 공개자료에도 없고(소재지 산암리 150-7만 존재),
  UPA 부두현황은 취급화물을 '조선기자재/규사'(벌크)로 표기해 해수부 805의 '유류'와 상충 → 벌크 가능성이 높아 제외.
  (대안으로 소재지 근사좌표 lat 35.44250 / lon 129.35950 으로 context 유지하는 방안도 있음 — 필요 시 복원 가능)
- **S-Oil SPM 부이 2기(BU-SOIL, BU-OILHUB)는 좌표 공란 확정**, `coord_source='해도필요_신항앞해상SPM'`.
  오프쇼어 원유 부이라 ADJACENT_TO 대상이 아니어서 로직 영향 없음.
- 재생성·재적재 결과: Berth **14** / ADJACENT_TO 20(변화 없음 — 달포는 원래 엣지 없었음) /
  SUBSTITUTABLE_WITH 18 / FALLBACK_ANCHORAGE 11(달포→E1 엣지 제거) / 단독선석(대체불가) = 효성, 석유공사부이 2곳.
  잔여 좌표 결측 경고는 부이 2기뿐이며 이는 확정 상태(해도 확보 시에만 갱신).
- 검증 스위트 재실행: 배정 로직 10/10, 안전 게이트 18/18 전부 PASS 유지.
